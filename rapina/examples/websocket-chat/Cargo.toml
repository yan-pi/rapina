[package]
name = "websocket-chat"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
rapina = { path = "../..", features = ["websocket"] }
tokio = { version = "1", features = ["full"] }
tracing-subscriber = "0.3"
bytes = "1"
http-body-util = "0.1"
